<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client" 
        xmlns:view="clr-namespace:Client.Views"
        mc:Ignorable="d"
        Title="MainWindow" MinHeight="150" MinWidth="500"
        SizeToContent="WidthAndHeight"
        MaxHeight="800" MaxWidth="1200" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
        <view:LoginView></view:LoginView>
        <ContentControl Content="{Binding SelectedViewModel}"/>
        <Button Grid.Column="1" Grid.Row="1" Content="GoToView" Command="{Binding UpdateView, UpdateSourceTrigger=PropertyChanged}" CommandParameter="ChatView"/>
    </Grid>
    
    
</Window>
