<UserControl x:Class="Client.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Views"
             xmlns:viewModel="clr-namespace:Client.ViewModels"
             xmlns:interactivity="http://prismlibrary.com/"
             interactivity:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" MinWidth="400" MinHeight="300"
             d:DataContext="{d:DesignInstance viewModel:ChatViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
            <TextBlock Text="Temporar general chat for testing purpose" />
        </StackPanel>

        <ListBox Grid.Row="1" Grid.Column="0">
            <ListBoxItem HorizontalAlignment="Center">
                <TextBlock Text="Users">
                </TextBlock>
            </ListBoxItem>
        </ListBox>
        
        <!--<ListBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5"/>
        <TextBox Grid.Row="2" Grid.Column="0" Text="{Binding TypingText}"/>
        <Button Grid.Row="2" Grid.Column="1" Content="Send" Command="{Binding SendMessage}" />-->


    </Grid>
</UserControl>
